export interface Task {
                        id: number;
                        title: string;
                        details: string;
                        date: Date;
                        is_done: boolean;
                    }